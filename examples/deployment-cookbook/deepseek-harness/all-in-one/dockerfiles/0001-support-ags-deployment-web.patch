diff --git a/packages/bundle/web-app/src/startup.ts b/packages/bundle/web-app/src/startup.ts
index 9faad24..3e767f5 100644
--- a/packages/bundle/web-app/src/startup.ts
+++ b/packages/bundle/web-app/src/startup.ts
@@ -65,3 +65,4 @@ Examples:
- * command's action publishes the flags this invocation named; `--host 0.0.0.0`
- * or a non-numeric `--port` is a usage error, so on rejection (and on `--help`)
- * nothing is provided.
+ * command's action publishes the flags this invocation named; a non-numeric
+ * `--port` is a usage error, so on rejection (and on `--help`) nothing is
+ * provided. Deployments may explicitly bind `0.0.0.0`; callers are responsible
+ * for constraining network reachability and declaring trusted authorities.
@@ -74,3 +74,0 @@ export function apply(ctx: Context): void {
-    if (options.host === '0.0.0.0') {
-      program.error('error: --host 0.0.0.0 is intentionally not supported yet for safety: it would expose remote code execution to the network; use 127.0.0.1 instead')
-    }
diff --git a/packages/bundle/web-app/tests/startup.spec.ts b/packages/bundle/web-app/tests/startup.spec.ts
index ba38062..c848962 100644
--- a/packages/bundle/web-app/tests/startup.spec.ts
+++ b/packages/bundle/web-app/tests/startup.spec.ts
@@ -137 +137 @@ describe('web command-line provider', () => {
-  it('rejects the intentionally unsupported all-interfaces host before the consumer activates', async () => {
+  it('publishes an explicit all-interfaces host for a network-constrained deployment', async () => {
@@ -139,4 +139,8 @@ describe('web command-line provider', () => {
-    expect(observed.out).toContain('--host 0.0.0.0 is intentionally not supported yet for safety: it would expose remote code execution to the network; use 127.0.0.1 instead')
-    expect(values).toBeUndefined()
-    expect(observed.readerConfig).toBeUndefined()
-    expect(observed.exits).toEqual([1])
+    expect(values).toEqual({ host: '0.0.0.0', openBrowser: true, trustedHosts: [] })
+    expect(observed.readerConfig).toEqual({
+      host: '0.0.0.0',
+      openBrowser: true,
+      port: 3080,
+      trustedHosts: [],
+    })
+    expect(observed.exits).toEqual([])
diff --git a/packages/client/connection/src/api-request-trust.ts b/packages/client/connection/src/api-request-trust.ts
index ea8914c..eaa0b1b 100644
--- a/packages/client/connection/src/api-request-trust.ts
+++ b/packages/client/connection/src/api-request-trust.ts
@@ -39,0 +40,16 @@ function parseAuthority(authority: string): URL | undefined {
+/**
+ * Parse the one wildcard shape accepted for deployment-generated hostnames.
+ * `*.example.com` matches exactly one left-most DNS label and any port. It is
+ * deliberately not a general glob: no wildcard may appear in the suffix and
+ * nested labels such as `a.b.example.com` do not match.
+ */
+function wildcardSuffix(entry: string): string | undefined {
+  if (!entry.startsWith('*.')) return undefined
+  const suffix = entry.slice(2)
+  if (suffix === '' || suffix.includes('*') || suffix.includes(':')) return undefined
+  const suffixUrl = parseAuthority(suffix)
+  if (suffixUrl === undefined || suffixUrl.port !== '') return undefined
+  if (canonicalAuthority(suffix, suffixUrl) !== suffix.toLowerCase()) return undefined
+  return suffixUrl.hostname
+}
+
@@ -54,0 +71,4 @@ export function assertTrustedAuthority(entry: string): void {
+  if (wildcardSuffix(entry) !== undefined) return
+  if (entry.includes('*')) {
+    throw new Error(`client-connection: trustedHosts entry ${JSON.stringify(entry)} is not a bare host[:port] authority or a single-label wildcard`)
+  }
@@ -57 +77 @@ export function assertTrustedAuthority(entry: string): void {
-  throw new Error(`client-connection: trustedHosts entry ${JSON.stringify(entry)} is not a bare host[:port] authority`)
+  throw new Error(`client-connection: trustedHosts entry ${JSON.stringify(entry)} is not a bare host[:port] authority or a single-label wildcard`)
@@ -81,0 +102,7 @@ function isTrustedAuthority(hostUrl: URL, trustedHosts: readonly string[]): bool
+    const suffix = wildcardSuffix(entry)
+    if (suffix !== undefined) {
+      const prefix = hostUrl.hostname.endsWith(`.${suffix}`)
+        ? hostUrl.hostname.slice(0, -(suffix.length + 1))
+        : ''
+      return prefix !== '' && !prefix.includes('.')
+    }
@@ -93,2 +120,7 @@ function isTrustedAuthority(hostUrl: URL, trustedHosts: readonly string[]): bool
- * @param trustedHosts - non-loopback authorities this deployment serves: exact `host:port`, or port-less `host` matching any port.
- * @returns true when the Host is ours (loopback or trusted) and any attached browser markers are same-origin.
+ * @param trustedHosts - non-loopback authorities this deployment serves:
+ * exact `host:port`, port-less `host` matching any port, or `*.suffix`
+ * matching one left-most label on any port.
+ * @returns true when the Host is ours (loopback or trusted) and any attached
+ * browser Origin is either same-authority or another explicitly trusted HTTPS
+ * authority. The latter covers reverse proxies that replace Host while
+ * preserving the public browser Origin.
@@ -113,2 +145,4 @@ export function isTrustedApiRequest(request: ApiTrustRequest, trustedHosts: read
-  // authority (compared through the same normalization as the Host). Absent
-  // Origin is fine — the Host fence above already bound the request. The
+  // authority, or both authorities must be explicit trustedHosts entries. The
+  // second shape supports authenticated reverse proxies that preserve the
+  // public HTTPS Origin while replacing Host with a private upstream authority.
+  // Absent Origin is fine — the Host fence above already bound the request. The
@@ -119 +153,5 @@ export function isTrustedApiRequest(request: ApiTrustRequest, trustedHosts: read
-    return new URL(origin).host === hostUrl.host
+    const originUrl = new URL(origin)
+    if (originUrl.host === hostUrl.host) return true
+    return originUrl.protocol === 'https:'
+      && isTrustedAuthority(originUrl, trustedHosts)
+      && isTrustedAuthority(hostUrl, trustedHosts)
diff --git a/packages/client/connection/tests/api-request-trust.host.spec.ts b/packages/client/connection/tests/api-request-trust.host.spec.ts
index f145230..86d1de2 100644
--- a/packages/client/connection/tests/api-request-trust.host.spec.ts
+++ b/packages/client/connection/tests/api-request-trust.host.spec.ts
@@ -43,0 +44,42 @@ describe('isTrustedApiRequest', () => {
+  it('accepts only one left-most label for a declared deployment wildcard', () => {
+    const trusted = ['*.ap-shanghai.agents.tencentags.com']
+    expect(isTrustedApiRequest(request({
+      host: '3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+      origin: 'https://3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+    }), trusted)).toBe(true)
+    expect(isTrustedApiRequest(request({
+      host: 'nested.3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+      origin: 'https://nested.3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+    }), trusted)).toBe(false)
+    expect(isTrustedApiRequest(request({
+      host: 'ap-shanghai.agents.tencentags.com',
+      origin: 'https://ap-shanghai.agents.tencentags.com',
+    }), trusted)).toBe(false)
+    expect(isTrustedApiRequest(request({
+      host: '3080-dpl-a1b2c3.evil.example',
+      origin: 'https://3080-dpl-a1b2c3.evil.example',
+    }), trusted)).toBe(false)
+  })
+
+  it('accepts a trusted external Origin forwarded to a separately trusted internal Host', () => {
+    const trusted = [
+      '*.ap-shanghai.agents.tencentags.com',
+      '*.ap-shanghai.internal.tencentags.com',
+    ]
+    expect(isTrustedApiRequest(request({
+      host: '3080-instance-a1b2c3.ap-shanghai.internal.tencentags.com',
+      origin: 'https://3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+      'sec-fetch-site': 'same-origin',
+    }), trusted)).toBe(true)
+    expect(isTrustedApiRequest(request({
+      host: '3080-instance-a1b2c3.ap-shanghai.internal.tencentags.com',
+      origin: 'https://attacker.example',
+      'sec-fetch-site': 'same-origin',
+    }), trusted)).toBe(false)
+    expect(isTrustedApiRequest(request({
+      host: 'attacker.example',
+      origin: 'https://3080-dpl-a1b2c3.ap-shanghai.agents.tencentags.com',
+      'sec-fetch-site': 'same-origin',
+    }), trusted)).toBe(false)
+  })
+
@@ -71,2 +113,2 @@ describe('isTrustedApiRequest', () => {
-  it('assertTrustedAuthority accepts bare authorities and throws on anything more', () => {
-    for (const entry of ['harness.internal', 'harness.internal:3080', 'HARNESS.internal:80', '10.0.0.9', '[::1]:3080']) {
+  it('assertTrustedAuthority accepts bare authorities and single-label wildcards, then throws on anything more', () => {
+    for (const entry of ['harness.internal', 'harness.internal:3080', 'HARNESS.internal:80', '10.0.0.9', '[::1]:3080', '*.ap-shanghai.agents.tencentags.com']) {
@@ -78 +120 @@ describe('isTrustedApiRequest', () => {
-      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority/)
+      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority or a single-label wildcard/)
@@ -82 +124 @@ describe('isTrustedApiRequest', () => {
-      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority/)
+      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority or a single-label wildcard/)
@@ -88 +130,4 @@ describe('isTrustedApiRequest', () => {
-      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority/)
+      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority or a single-label wildcard/)
+    }
+    for (const entry of ['*', '*.', '*.*.example.com', 'api.*.example.com', '*.example.com:3080', '*.bad entry']) {
+      expect(() => { assertTrustedAuthority(entry) }).toThrow(/not a bare host\[:port\] authority or a single-label wildcard/)
