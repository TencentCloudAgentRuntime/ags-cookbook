apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - manifest.yaml

configMapGenerator:
  - name: k6-helloworld
    files:
      - script.js
    options:
      disableNameSuffixHash: true
