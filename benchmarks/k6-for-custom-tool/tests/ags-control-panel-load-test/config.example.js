// ============================================================================
// 配置文件示例 - 复制此文件为 config.js 并修改配置值
// ============================================================================

export const config = {
    // ------------------------------------------------------------------------
    // AGS 配置
    // ------------------------------------------------------------------------
    toolName: 'your-tool-name',
    instanceTimeout: '5m',

    // ------------------------------------------------------------------------
    // 压测参数 (constant-arrival-rate 模式)
    // ------------------------------------------------------------------------
    // 每秒请求数 (QPS)
    qps: 10,
    // 测试持续时间
    duration: '30m',
    // 预分配 VU 数
    preAllocatedVUs: 50,
    // 最大 VU 数 (应对慢请求，需足够大以维持目标 RPS)
    maxVUs: 200,
    // 异步停止延迟秒数 (实际延迟会在 ±20% 范围内随机波动)
    stopDelaySeconds: 60,

    // ------------------------------------------------------------------------
    // 镜像列表 (设为空数组则不使用镜像)
    // ------------------------------------------------------------------------
    images: [
        // "your-image:tag",
    ],
};
